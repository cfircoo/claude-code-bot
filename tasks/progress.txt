# Ralph Progress Log
Started: Sun Feb  1 08:19:22 IST 2026
---

## US-201: Add extra="ignore" to BotConfig — DONE
- Added `model_config = ConfigDict(extra="ignore")` to BotConfig
- Added test `test_bot_config_extra_fields_ignored`
- Pre-existing failures: test_agent.py import error (CONVERSATION_INSTRUCTION removed), 5 test failures in test_cli.py/test_telegram.py
- All config tests pass (8/8)

## US-202: Add show_tool_activity config option — DONE
- app.py lifespan reads show_tool_activity from telegram channel settings
- config.example.yaml updated with commented-out example
- Added 2 tests for default value and setting toggle
- Pre-existing telegram test failures unchanged (3 failures)

## US-203: Add --api-key flag to CLI agent.py — DONE
- Added --api-key CLI argument, passes X-API-Key header to health check and streaming requests
- send_streaming now accepts optional headers param
- Added test_api_key_header_sent verifying header is passed through
- Pre-existing CLI test failures unchanged (2 failures: test_text_tokens_printed_incrementally, test_bad_json_in_sse_stream)

## US-204: Add ANSI color formatting to CLI — DONE
- Added DIM, RED, CYAN, BOLD, RESET ANSI constants with TTY detection
- Tool indicators in dim, errors in red, conversation switch in cyan
- Colors disabled when stdout is not a TTY
- Added 3 tests verifying no ANSI codes in non-TTY output

## US-205: Persist conversation_id in CLI interactive mode — DONE
- Server now emits conversation_id in result event
- send_streaming returns conversation_id (str | None)
- Interactive loop captures first conversation_id and reuses for subsequent messages
- --conversation flag still overrides
- Added 2 tests: returns conversation_id, returns None when missing

## US-206: Split long Telegram responses — DONE
- Added split_message() function with paragraph > newline > space > hard cut priority
- _process_with_streaming sends chunks with 0.3s delay between them
- Added 7 tests covering all split scenarios
- Pre-existing telegram test failures unchanged (3 failures)

## US-207: Add retry on connection drop during CLI streaming — DONE
- Wrapped streaming in retry loop (max 2 retries) for ReadError/RemoteProtocolError
- Shows [Reconnecting...] on retry, final error after exhaustion
- Added 2 tests: successful retry, exhausted retries

## US-208: Add thinking indicator for long Telegram agent runs — DONE
- Added thinking_threshold attribute (default 10s), configurable via channel settings
- asyncio task sends "Thinking..." after threshold, gets cancelled if response arrives first
- If thinking was sent, first chunk edits the thinking message instead of new message
- Added 3 tests: default threshold, configurable threshold, fast response no thinking

## US-209: Add persistent memory system — DONE
- Created src/claude_code_bot/memory_store.py with MemoryStore class
- Auto-creates memory/core/ and memory/to_improve/ dirs
- load_core(), load_all(), write(), delete(), list() methods with core/ protection and path traversal prevention
- Added memory_path to BotConfig (default ~/.claude-bot/memory)
- Integrated into AgentService._build_system_prompt() and app.py lifespan
- Added 13 tests covering all acceptance criteria
- config.example.yaml updated

## US-210: Memory MCP tools — BLOCKED
- SDK ProcessTransport bug still prevents MCP tools from working
- MemoryStore from US-209 works via system prompt injection as fallback

## US-211: Update system prompt with memory instructions — DONE
- Updated config.example.yaml system_prompt with memory system instructions
- Instructions cover all zones, guidelines, and proactive saving
