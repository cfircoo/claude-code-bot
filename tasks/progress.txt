# Ralph Progress Log
Started: Sat Jan 31 23:50:30 IST 2026
---

## US-101: Replace memory.py with ConversationStore - DONE
- Replaced Message/MemoryBackend/JsonFileMemoryBackend with ConversationMeta/ConversationStore
- Storage: data/conversations/{safe_user_id}.json as JSON array
- Updated agent.py: simplified chat() method, uses ConversationStore (store param instead of memory)
- Updated app.py: uses ConversationStore() directly instead of create_memory_backend()
- Updated telegram.py: removed memory persistence in proactive messages (deferred to US-106)
- Removed old test_memory.py, rewrote test_agent.py, added test_conversation_store.py (13 tests)
- All 44 tests pass, mypy clean, ruff clean
- Note: agent.py chat() is simplified (no history tracking) — US-103 will fully rewrite it
- Note: MemoryConfig still exists in config.py — US-107 will clean it up

## US-102: Create conversation management MCP tools - DONE
- Created src/claude_code_bot/tools.py with 4 MCP tools: list, create, switch, delete
- Uses @tool decorator and create_sdk_mcp_server() from claude_agent_sdk
- Tools access ConversationStore via closure over store and user_id
- _make_tools() helper exposed for testability (returns list of SdkMcpTool)
- create_conversation_tools() returns McpSdkServerConfig (a dict with type="sdk")
- Note: McpSdkServerConfig is actually a TypedDict (dict), not a class instance
- Note: SdkMcpTool has .handler attribute for the async callable, .name for tool name
- 10 new tests in test_tools.py, all 54 tests pass, mypy clean, ruff clean

## US-103: Rewrite AgentService with streaming and session resume - DONE
- Rewrote agent.py with chat_stream() async generator yielding event dicts
- Event types: text, tool_start, tool_done, result, error, conversation_switched
- include_partial_messages=True for StreamEvent streaming
- Session resume: passes resume=session_id to ClaudeAgentOptions when meta.session_id exists
- Captures session_id from SystemMessage(subtype="init") and ResultMessage
- Persists session_id and last_active to ConversationMeta after each interaction
- Auto-creates conversation if none exists, uses most recent if conversation_id is None
- MCP conversation tools registered via mcp_servers with allowed_tools
- System prompt includes conversation management instruction
- Retry with exponential backoff (3 attempts)
- Added backward-compatible chat() method that consumes chat_stream()
- 15 new tests in test_agent.py (was 4), all 65 tests pass, mypy clean, ruff clean
- SDK types: StreamEvent.event is a raw dict, AssistantMessage.content has TextBlock/ToolUseBlock
- ClaudeAgentOptions.mcp_servers accepts dict[str, McpServerConfig] (McpSdkServerConfig is a subtype)
