# Ralph Progress Log
Started: Sat Jan 31 23:50:30 IST 2026
---

## US-101: Replace memory.py with ConversationStore - DONE
- Replaced Message/MemoryBackend/JsonFileMemoryBackend with ConversationMeta/ConversationStore
- Storage: data/conversations/{safe_user_id}.json as JSON array
- Updated agent.py: simplified chat() method, uses ConversationStore (store param instead of memory)
- Updated app.py: uses ConversationStore() directly instead of create_memory_backend()
- Updated telegram.py: removed memory persistence in proactive messages (deferred to US-106)
- Removed old test_memory.py, rewrote test_agent.py, added test_conversation_store.py (13 tests)
- All 44 tests pass, mypy clean, ruff clean
- Note: agent.py chat() is simplified (no history tracking) — US-103 will fully rewrite it
- Note: MemoryConfig still exists in config.py — US-107 will clean it up

## US-102: Create conversation management MCP tools - DONE
- Created src/claude_code_bot/tools.py with 4 MCP tools: list, create, switch, delete
- Uses @tool decorator and create_sdk_mcp_server() from claude_agent_sdk
- Tools access ConversationStore via closure over store and user_id
- _make_tools() helper exposed for testability (returns list of SdkMcpTool)
- create_conversation_tools() returns McpSdkServerConfig (a dict with type="sdk")
- Note: McpSdkServerConfig is actually a TypedDict (dict), not a class instance
- Note: SdkMcpTool has .handler attribute for the async callable, .name for tool name
- 10 new tests in test_tools.py, all 54 tests pass, mypy clean, ruff clean

## US-103: Rewrite AgentService with streaming and session resume - DONE
- Rewrote agent.py with chat_stream() async generator yielding event dicts
- Event types: text, tool_start, tool_done, result, error, conversation_switched
- include_partial_messages=True for StreamEvent streaming
- Session resume: passes resume=session_id to ClaudeAgentOptions when meta.session_id exists
- Captures session_id from SystemMessage(subtype="init") and ResultMessage
- Persists session_id and last_active to ConversationMeta after each interaction
- Auto-creates conversation if none exists, uses most recent if conversation_id is None
- MCP conversation tools registered via mcp_servers with allowed_tools
- System prompt includes conversation management instruction
- Retry with exponential backoff (3 attempts)
- Added backward-compatible chat() method that consumes chat_stream()
- 15 new tests in test_agent.py (was 4), all 65 tests pass, mypy clean, ruff clean
- SDK types: StreamEvent.event is a raw dict, AssistantMessage.content has TextBlock/ToolUseBlock
- ClaudeAgentOptions.mcp_servers accepts dict[str, McpServerConfig] (McpSdkServerConfig is a subtype)

## US-104: Add SSE streaming endpoint and conversation REST API - DONE
- Added POST /chat/stream SSE endpoint streaming events from chat_stream()
- SSE format: "data: {json}\n\n" for each event
- Added GET /conversations/{user_id} returning list of ConversationResponse
- Added POST /conversations/{user_id} with optional {name} body (201 status)
- Added DELETE /conversations/{user_id}/{conversation_id} (404 if not found)
- ConversationResponse model: conversation_id, name, created_at, last_active
- Removed old POST /chat endpoint (replaced test_chat.py with stub)
- Lifespan now stores ConversationStore in _store module var for CRUD endpoints
- 13 new tests in test_stream_and_conversations.py, all 73 tests pass, mypy clean, ruff clean
- Note: empty message on /chat/stream returns 400 (not 200 with fallback like old /chat)

## US-105: Update CLI agent.py with streaming display - DONE
- Rewrote agent.py to use POST /chat/stream SSE endpoint instead of old POST /chat
- Text tokens printed incrementally via sys.stdout.write/flush (no buffering)
- Tool use shown as: [Using ToolName...] done
- Subagent activity indented with ↳ when parent_tool_use_id present
- Final result delineated with ─ separator line
- Added --conversation flag to target specific conversation
- --debug shows raw SSE events and request details
- Interactive mode (-i) works with streaming
- Graceful handling of malformed JSON in SSE stream
- 13 new tests in test_cli.py, all 86 tests pass, mypy clean, ruff clean
- Uses httpx.stream() with 300s read timeout for long-running agent responses

## US-106: Update Telegram channel with typing indicator and streaming status - DONE
- Replaced agent.chat() calls with chat_stream() via _process_with_streaming()
- Typing indicator sent via _send_typing_loop() running concurrently with stream processing
- Typing action refreshed every 4 seconds (Telegram typing expires after ~5s)
- Tool activity optionally shown in response via show_tool_activity flag (default: off)
- Conversation management works via natural language (agent's MCP tools handle it)
- Error events shown to user with "Error: " prefix
- Conversation switched events shown when tool activity is enabled
- "No response." fallback when stream yields no result event
- Removed old US-106 TODO comment from proactive_message
- Added ChatAction import from aiogram.enums
- 5 new tests (streaming result, tool activity, error, conversation switch, no response), all 91 tests pass, mypy clean

## US-107: Clean up old code and update config - DONE
- Removed MemoryConfig class from config.py
- Removed memory field from BotConfig model
- Removed memory section from config.yaml and config.example.yaml
- Updated test_config.py to remove assertion on config.memory.backend
- No old test_memory.py files existed (already removed in US-101)
- All imports reference new ConversationStore/ConversationMeta (no old MemoryBackend refs)
- All 91 tests pass, ruff clean, mypy clean
